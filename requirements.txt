# Versions used to produce the results reported in the paper
# (Python 3.14, Windows 11; the pipeline also runs on macOS and Linux).
pandas==3.0.3
numpy==2.4.4
matplotlib==3.10.9
scikit-learn==1.9.0
openmeteo-requests==1.7.5
requests-cache==1.3.3
retry-requests==2.0.0
